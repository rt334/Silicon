# Silicon

> 又称"更好的像素工厂"（
>
> 你是否被左脚踩右脚的矿物开采气晕？你是否试过电力归零无法启动的绝望？
>
> 使用硅，解决以上问题！——锐意更新中

一个添加了个人创意的 Mindustry 模组（`v159.7+`），提供多种实用方块与便捷功能。

## 方块与物品

| 方块 | 说明 |
|------|------|
| 矿物转换器 | 按地图矿点稀有度，把可开采的矿物相互转化 |
| 物品传输中枢 | 零缓冲物流网络：按需拉取、盈余自动推送至核心，可跨中枢级联 |
| 电力保护器 | 电网崩溃时锁定电量并进入保护，记录消耗电力后在恢复模式下逐步偿还 |
| 利滚利发电机 | 按已存电力的 1%/秒复合产出电力，产出随电网状态自适应 |
| 发电泵 | 抽水同时消耗水发电 |
| 两用交叉器 | 传送带 / 导管两用的交叉路口 |
| 两用仓储 | 3×3 仓储与合成一体，配套液体行为 |
| 万向交叉器 | 1×1 物品交叉器：全局设置输出优先级应用到所有输入方向，可按方向单独覆盖；内置模板一键应用（均分/全右/主右备左/上下直通）、可手动保存自定义模板（0~4，数值越大越优先，同优先级轮流均分，0 不输出），50 物品/秒，点击方块在配置面板中直接设置 |
| 信号源 | 放置后注册唯一信号（4 个字母或数字，绑定队伍），半径 15 格广播，强度随距离正态衰减（0~15，σ=6），按住 H 键查看覆盖 |
| 信号中继器 | 位于信号覆盖范围内自动激活，自身提供同样半径 15 格的信号，可级联延长覆盖（强度同样随距离衰减） |
| 卫星发射中枢 | 生产待发射卫星（消耗石油与缓冲电力 10000），生产完成登记后等待控制台发射 |
| 卫星控制台 | 发射卫星：绑定信号后指派轨道（LEO / MEO / GEO / SSO），按轨道扣燃油；与发射中枢 1:1 配对 |
| 开关 | 控制指向方块的启用/禁用 |
| 电力源 | 沙盒无限电源（与电网内其他电力源平分产出） |

## 功能

- **方块搜索**：建造菜单内跨全分类搜索方块，支持中文拼音模糊匹配（全拼/首字母）、搜索历史记录；
- **多人暂停**：多人游戏中可申请暂停，支持关闭/仅管理员/自定义白名单三种模式；
- **信号覆盖显示**：按住 H 键（或设置切换为按一下切换）查看信号强度——视野宽时显示蓝色范围、视野窄时逐格显示强度数字，淡入淡出过渡，数字/范围透明度可调；
- **自动更新**：主界面自动检测 GitHub 新版本并弹窗提示，支持游戏内下载安装到模组目录后一键重启。

## 卫星系统（第一阶段）

卫星是**真实实体单位**（机型按轨道划分：LEO / MEO / GEO / SSO），沿以地图为中心的圆轨道飞行，轨道越高越慢、覆盖越大——轨道用于划分不同卫星的功能，且轨道越高越难被地面击中（影响未来拦截难度）：

| 轨道 | 燃油（石油） | 覆盖半径 | 周期 |
|------|------------|---------|------|
| LEO | 1000 | 30 格 | 100s |
| MEO | 2500 | 45 格 | 160s |
| GEO | 5000 | 60 格 | 240s |
| SSO | 8000 | 20 格 | 60s |

- **测试卫星**：与信号卫星效果完全相同，仅需 1 硅、1 秒生产，可发射至任意轨道（信号卫星限 LEO/MEO/GEO，SSO 不对信号卫星开放）——用于验证发射链路或廉价快速叠星；**仅在沙盒模式可用**，其他模式不出现（中枢不显示该选项、不生产，发射请求被权威端拒绝）；
- **覆盖与强度**：卫星为其所属编码提供星下点覆盖圆；覆盖圆内每星贡献强度 1.5，按发射时固化的信道扣除干扰后求和、再扣底噪 0.5——首颗有效强度 1.0，单星即可激活中继器转发（阈值 >0.5），叠星线性提升抗干扰裕度；
- **干扰**：同信道干扰器可局部压制卫星的覆盖与绑定（压制格在 H 键覆盖中不显示）；
- **远方指派**：卫星覆盖可解锁地面信号范围外的控制台绑定，但中枢/控制台 1:1 配对仍只统计地面覆盖；
- **不可操控 / 不可攻击**：玩家与逻辑处理器均无法操控，不会被任何单位索敌、伤害或碰撞（含其他卫星与空中单位），小地图不显示过境；
- **持久化**：卫星随存档保存，轨道相位续接不跳位；名册（编码/信道/轨道/相位）由卫星控制台存档块代存，任一存活控制台即可恢复，控制台全毁则卫星退化为未绑定（仅覆盖强度，不参与绑定）；
- **预留**：卫星血量 400，目前只能被脚本化伤害击落——ASAT 拦截手段在后续阶段接入。

## 设置

进入游戏后：`设置 → Silicon设置`。

| 设置 | 说明 |
|------|------|
| 显示搜索历史 | 是否在方块搜索栏中显示最近搜索（可游戏内即时开关） |
| 选择后清除输入 | 从搜索结果选择方块后是否清空搜索输入 |
| 暂停模式 | 多人暂停权限：关闭 / 仅管理员 / 自定义（聊天命令 `!pause grant <玩家名>`） |
| 暂停请求 | 启用后按下暂停键向服务器发送暂停申请 |
| 管理暂停白名单 | 打开自定义暂停模式的白名单管理对话框 |
| 信号显示切换 | H 键按住显示 / 按一下切换显示 |
| 数字模式透明度 | 信号数字模式的显示透明度（0~100%） |
| 范围模式透明度 | 信号范围模式的显示透明度（0~100%） |
| 中枢调试日志 | 物品传输中枢的调试日志输出（默认关闭） |
| 中枢连线透明度 | 物品传输中枢连接线的透明度（0~100%） |
| 自动更新检查 | 启动时自动检查 GitHub 新版本 |
| 检查更新 | 立即向 GitHub 重新检查一次更新（有更新弹窗提示，已最新则提示） |

## 构建与固化验证

依赖 Mindustry `v159.7` 的 jar 与 **JDK 17**（Android 端 d8 需要）：

```powershell
# 一条龙：30 项代码锚点检查 → 编译 deploy → 自动同步游戏模组目录
powershell -ExecutionPolicy Bypass -File scripts\hub-deep-check.ps1

# 仅桌面快速构建
.\gradlew.bat jar
```

安装：把构建出的 `Silicon.jar` 放入游戏的 `mods` 目录（客户端：`%APPDATA%/Mindustry/mods`）后启动游戏。

## 开发流程（固化）

1. 修改代码后先跑单元测试（`tests/` 下三个套件，javac 直接编译运行，不依赖 Mindustry 运行时）
2. 运行 `scripts/hub-deep-check.ps1`：任一锚点 FAIL 即阻断；通过则自动 deploy 并把最新 jar 同步到 `%APPDATA%\Mindustry\mods\Silicon.jar`
3. 重启游戏实测
4. 版本号按 AGENTS.md 规范 bump，commit 格式 `[版本] 类型: 描述`

## 链接

- 仓库：<https://github.com/Xiaobei08/Silicon>
- 方块搜索功能与 Silicon 共用实现（`silicon.ui.BlockSearch`），也可作为独立模组打包使用

## 更新日志

### a0.13.0.0「信号与卫星 · 第一阶段」（最新）
- **卫星实体化**：卫星从抽象计数改为真实实体单位——机型按轨道划分（LEO/MEO/GEO/SSO），沿地图中心圆轨道飞行；位置=相位+时间的纯函数，读档续接不跳位；玩家/逻辑不可操控，不可被索敌、伤害或碰撞，小地图不显示过境
- **信号语义重构**：卫星覆盖从"发射即全图"改为星下点覆盖圆（30/45/60/20 格按轨道），每星强度 1.5、按发射时固化信道扣干扰、总和扣底噪——单星有效强度 1 可激活中继器转发，叠星提升抗干扰裕度；同信道干扰器可局部打断卫星绑定
- **发射流程**：控制台绑定信号 → 地面 1:1 配对中枢/控制台（卫星覆盖解锁"远方指派"但不参与配对）→ 按轨道扣燃油与缓冲电力 → 实体入轨
- **修复发射自锁**：多中枢队伍不再被自己的卫星锁死发射（配对计数改地面覆盖）
- **持久化**：卫星实体随存档保存；名册（编码/信道/轨道/相位）由卫星控制台存档块代存（v2，旧档兼容），控制台全毁则卫星退化为未绑定
- H 键覆盖：卫星档逐星绘制星下点覆盖盘（编码专属色、被压制格不画），数字模式卫星强度与地面信号同模型合成
- 预留：卫星血量 400，仅脚本化伤害可击落（ASAT 拦截在后续阶段）
- 测试卫星沙盒专属：非沙盒模式不出现（中枢隐藏选项且不生产、发射请求被权威端拒绝并提示）

### a0.12.0.0
- 版本号更新至 a0.12.0.0，同步更新 README（信号系统正式版说明）

### a0.11.0.1
- **信号系统重构**：移除旧信号系统（旧信号源/维度锚点，维度锚点保留为隐藏存根以兼容旧存档），全新信号源（信号类 4 字符 + 强度 0~15，绑定队伍，半径 15 格正态衰减 σ=6）+ 信号中继器（级联延长覆盖）+ H 键覆盖显示（视野宽=蓝色范围、视野窄=逐格强度数字，淡入淡出，数字/范围透明度可调）
- 矿物转换器进度条文本「采矿中」→「进度」（#9 @by514）
- 信号性能优化：按队缓存（避免每帧遍历建筑）、视野裁剪、中继器网络/存档状态同步、代码审查修复

### a0.11.0.0
- **新增方块：两用仓储 DualPurposeStorager**——仓储与合成一体，配套液体行为测试
- **物品传输中枢连接系统重构**：
  - 连接单一归属：一个建筑只被一个中枢服务，认领即从其它中枢整体抢回连接与挂起项，杜绝重复/竞争连接
  - 满员回退就近接入：最近枢配额满自动落到范围内更远的有空位枢；容量闸门先于归属裁决
  - 复制拓扑保真：原理图 / F 键复制严格复刻原始连接模式，同时自动互连范围内全部中枢（粉色骨架）
  - 建造完成事件增量接入 + 挂起补连（600s 窗口），后建建筑不漏连
  - 放置侧连接预览（偶数方块半格锚点对齐原版）；F 复制 / 原理图预览 postDraw 统一渲染，不再被任何方块遮挡
- 计费与统计固化：每枢一跳计费、瞬时请求平滑、耗电按秒统计、速率 10s 窗口；电力不足完全停转 + 探测恢复
- 重构工厂纳入供料；连接白名单收紧；调度节流 6Hz；连接存档序列化 v2（中枢间粉线独立计数）
- 放置/单击三色标记统一：蓝=直连、紫=同网、绿=可新建

### a0.10.1.0
- 新增**自动更新检查**：主界面自动检测 GitHub 新版本并弹窗提示，支持游戏内下载安装（替换 mods 目录旧 jar）后一键重启；设置页新增「检查更新」按钮可手动重新检查
- 优化：更新弹窗边框在深色背景上可见（改用 pane 九宫格贴图）；弹窗版本号显示不带 v 前缀

### a0.10.0.0
- 新增**信号系统**：信号源（放置生成唯一 4 位信号，通电有效）+ 维度锚点（3×3 发送/接收模式，按信号配对跨图传输，连线可视化）
- 修复：万向交叉器注册补回（此前合并误删导致无法建造）

### a0.9.0.0
- 新增方块：**万向交叉器**——1×1 物品交叉器，全局输出优先级 + 按方向覆盖两层配置，内置模板（均分/全右/主右备左/上下直通）一键应用，支持手动保存自定义模板（跨存档）；50 物品/秒按优先级输出，同优先级轮流均分，满载延迟降级防抖动
- 修复物品传输中枢旧存档读取时链接数据错位的兼容性问题

### a0.8.7.0
- 调整设置顺序：显示搜索历史、选择后清除输入 置于暂停设置之前
- 修复历史记录折叠时，搜索栏与方块选单之间的黑色边框
- 更新模组简介与 README

### a0.8.6.0
- **矿物转换器 · 多人同步**：世界矿物成本由服务端统一计算并广播给所有客户端，房主与其他玩家的显示与行为完全一致
- **矿物转换器 · 新地图清理**：进入新地图自动清除上一地图的矿物成本、选择列表与 `lastConfig`，新放置的转换器不再继承旧选择
- **方块搜索**：建造菜单内跨全分类搜索方块，支持中文拼音模糊匹配（全拼/首字母）、搜索历史与历史记录重复搜索
- **方块搜索设置**：新增"显示搜索历史""选择后清除输入"两个开关，整合进 Silicon 设置页，支持游戏内即时生效
- **搜索栏 UI**：背景与对齐方式与原版物品栏保持一致
- 物品传输中枢重写、暂停权限系统、方块设计文档等
